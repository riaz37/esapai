import { Play, Settings } from "lucide-react";
import type { LucideProps } from "lucide-react";

export function PlayIcon(props: LucideProps) {
    return <Play {...props} />;
}

export function GearIcon(props: LucideProps) {
    return <Settings {...props} />;
}
